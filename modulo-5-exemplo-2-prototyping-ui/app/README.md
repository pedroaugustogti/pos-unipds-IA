# App — Pix Agendado

Implementação do protótipo será gerada aqui na **Trilha B (Cursor)** usando `prompts/figma-to-code.md` e os artefatos do Exemplo 1.

```bash
# Após gerar o código:
npm install
npm run dev
```

Na **Trilha A (Firebase Studio)**, o protótipo fica hospedado na nuvem — esta pasta pode permanecer vazia ou receber export do código posteriormente.
