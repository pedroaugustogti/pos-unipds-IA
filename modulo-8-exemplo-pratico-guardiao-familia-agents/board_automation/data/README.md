# data — dados estáticos do board

| Subpasta | Conteúdo |
|----------|----------|
| `maps/` | CSV `TASK_AGENT_MAP*` (roteamento agente) |
| `imports/` | JSON import GitHub Project |
| `backlogs/` | Backlogs operacionais (sandbox, Fargate) |

Decisão: alterar roteamento → editar CSV + `classify_tasks.py` / reconcile.
