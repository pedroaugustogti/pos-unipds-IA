# Copie para secrets.yaml e substitua GROQ_API_KEY (não commitar secrets.yaml com chave real).
# Gerar base64: [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes("gsk_..."))
apiVersion: v1
kind: Secret
metadata:
  name: nexus-secrets
type: Opaque
data:
  GROQ_API_KEY: <SUA_CHAVE_EM_BASE64>
